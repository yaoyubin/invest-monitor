# 投资雷达
python-dotenv>=1.0
feedparser>=6.0
yfinance>=0.2.40
requests>=2.28
beautifulsoup4>=4.12
lxml>=4.9
PyYAML>=6.0
# LLM 评分（任选其一即可，按 SCORER_LLM_PROVIDER 决定使用哪个）
openai>=1.0
# anthropic>=0.40
# google-generativeai>=0.7
# 雪球大V抓取（可选；invest/xueqiu.py 用）
# 第一次安装后需执行 `playwright install chromium` 下载浏览器
playwright>=1.40
